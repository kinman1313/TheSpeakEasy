// Temporary placeholder for sound functionality
export const playSound = (type, variant = 'default') => {
    // Sound functionality temporarily disabled
    console.log('Sound play requested:', { type, variant });
}; 